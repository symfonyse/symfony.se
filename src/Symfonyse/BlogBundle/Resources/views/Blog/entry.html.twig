{% extends "SymfonyseBlogBundle::base.html.twig" %}

{% block title %}
{{entry.title }} — {{parent()}}
{% endblock %}

{% block content %}
    <article>
        <header>
            <h1><a href="{{path('blog', {'permalink': entry.permalink})}}">{{entry.title}}</a></h1>
        </header>
        {{ entry.content|markdown }}
        <div>
            <p><small>Published: {{ _misc.date(entry.postedAt, 'long') }} |
                    In: {{ _misc.tags(entry.tags) }}
                </small></p>
        </div>
    </article>
{% endblock %}
